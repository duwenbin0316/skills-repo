{
  "name": "skills-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run skills:sync && vite",
    "build": "npm run skills:sync && vite build",
    "preview": "vite preview",
    "skills:sync": "node scripts/generate-skills-json.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.10"
  }
}
